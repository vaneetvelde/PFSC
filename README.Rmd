---
output: github_document
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  eval = FALSE,
  comment = "#>",
  fig.path = "man/figures/README-",
  out.width = "100%"
)
library(knitr)
```

# PFSC: Probabilistic Final Standing Calculator

The PFSC package provides functions to simulate the remainder of prematurely stopped soccer leagues. It calculates for each team the probabilities to reach each position in the final league table.

# Istallation

The package can be installed via the following command, if you have the devtools package installed.

```{r, echo=T, eval=F}
devtools::install_github('vaneetvelde/PFSC')
```

# Functions

The three main functions are

1. league_table: To construct the official league table given a set of matches
2. team_ratings
